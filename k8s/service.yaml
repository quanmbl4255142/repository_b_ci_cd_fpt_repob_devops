apiVersion: v1
kind: Service
metadata:
  name: django-api-service
  namespace: django-api
  labels:
    app: django-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: django-api
