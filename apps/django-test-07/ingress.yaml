apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: django-test-07-ingress
  namespace: django-test-07
  labels:
    app: django-test-07
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - django-test-07.yourdomain.com
    secretName: django-test-07-tls
  rules:
  - host: django-test-07.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: django-test-07-service
            port:
              number: 8000
