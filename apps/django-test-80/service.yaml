apiVersion: v1
kind: Service
metadata:
  name: django-test-80-service
  namespace: django-test-80
  labels:
    app: django-test-80
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: django-test-80
