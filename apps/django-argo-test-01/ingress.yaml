apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: django-argo-test-01-ingress
  namespace: django-argo-test-01
  labels:
    app: django-argo-test-01
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - django-argo-test-01.yourdomain.com
    secretName: django-argo-test-01-tls
  rules:
  - host: django-argo-test-01.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: django-argo-test-01-service
            port:
              number: 8000
