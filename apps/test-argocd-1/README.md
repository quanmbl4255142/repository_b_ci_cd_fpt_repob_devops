# test-argocd-1 - Kubernetes Manifests

Generated by Django Dev Portal

## 📦 Deployment Information

- **Application**: test-argocd-1
- **Namespace**: test-argocd-1
- **Docker Image**: ghcr.io/quanmbl4255142/test_argocd_1
- **Port**: 8000

## 📁 Files

- `namespace.yaml` - Kubernetes namespace
- `deployment.yaml` - Application deployment with 2 replicas
- `service.yaml` - ClusterIP service
- `pvc.yaml` - PersistentVolumeClaim (1Gi)
- `ingress.yaml` - Ingress configuration
- `kustomization.yaml` - Kustomize configuration
- `argocd-application.yaml` - ArgoCD Application definition

## 🚀 Deployment to Repository_B

### Step 1: Add to Repository_B

```bash
# Clone Repository_B
git clone https://github.com/quanmbl4255142/Repository_B_CI_CD_FPT_repoB_Devops.git
cd Repository_B

# Create app directory
mkdir -p apps/test-argocd-1

# Copy manifests
cp <this-directory>/*.yaml apps/test-argocd-1/
cp argocd-application.yaml argocd-apps/test-argocd-1-app.yaml

# Commit and push
git add apps/test-argocd-1/ argocd-apps/test-argocd-1-app.yaml
git commit -m "Add test-argocd-1 application"
git push origin main
```

### Step 2: Deploy with ArgoCD

```bash
# Apply ArgoCD Application
kubectl apply -f argocd-apps/test-argocd-1-app.yaml

# Check status
kubectl get applications -n argocd-new

# Watch sync
kubectl get app test-argocd-1-app -n argocd-new -w
```

### Step 3: Verify Deployment

```bash
# Check pods
kubectl get pods -n test-argocd-1

# Check service
kubectl get svc -n test-argocd-1

# Check ingress
kubectl get ingress -n test-argocd-1

# View logs
kubectl logs -f deployment/test-argocd-1 -n test-argocd-1
```

## 🔄 Auto-Deployment Workflow

1. Push code to your app repository
2. GitHub Actions builds Docker image
3. Image pushed to registry: ghcr.io/quanmbl4255142/test_argocd_1
4. ArgoCD Image Updater detects new image
5. Updates deployment.yaml in Repository_B
6. ArgoCD syncs changes
7. New pods deployed automatically

## 🛠️ Manual Operations

### Force sync
```bash
kubectl patch app test-argocd-1-app -n argocd-new \
  -p '{"metadata":{"annotations":{"argocd.argoproj.io/refresh":"hard"}}}' \
  --type merge
```

### Restart deployment
```bash
kubectl rollout restart deployment/test-argocd-1 -n test-argocd-1
```

### Scale replicas
```bash
kubectl scale deployment/test-argocd-1 -n test-argocd-1 --replicas=3
```

### Delete application
```bash
kubectl delete app test-argocd-1-app -n argocd-new
kubectl delete namespace test-argocd-1
```

## 📊 Monitoring

### Health Check
```bash
curl http://<ingress-host>/api/health/
```

### Resource Usage
```bash
kubectl top pods -n test-argocd-1
```

### Events
```bash
kubectl get events -n test-argocd-1 --sort-by='.lastTimestamp'
```

## 🔧 Customization

### Update replicas
Edit `deployment.yaml`:
```yaml
spec:
  replicas: 3  # Change this
```

### Update resources
Edit `deployment.yaml`:
```yaml
resources:
  requests:
    memory: "512Mi"  # Increase
    cpu: "500m"
  limits:
    memory: "1Gi"
    cpu: "1000m"
```

### Update domain
Edit `ingress.yaml`:
```yaml
spec:
  rules:
  - host: your-custom-domain.com  # Change this
```

## 📚 Documentation

- [Repository_B Multi-App Guide](../GIAI-PHAP-REPOSITORY-B.md)
- [ArgoCD Documentation](https://argo-cd.readthedocs.io/)
- [Kubernetes Documentation](https://kubernetes.io/docs/)

---

**Generated**: 2025-10-10 06:39:47  
**Tool**: Django Dev Portal v1.0.0
