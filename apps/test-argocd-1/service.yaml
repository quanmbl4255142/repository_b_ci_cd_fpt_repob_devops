apiVersion: v1
kind: Service
metadata:
  name: test-argocd-1-service
  namespace: test-argocd-1
  labels:
    app: test-argocd-1
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: test-argocd-1
