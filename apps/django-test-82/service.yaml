apiVersion: v1
kind: Service
metadata:
  name: django-test-82-service
  namespace: django-test-82
  labels:
    app: django-test-82
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: django-test-82
