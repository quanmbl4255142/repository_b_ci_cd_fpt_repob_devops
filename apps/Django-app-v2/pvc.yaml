apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: Django-app-v2-pvc
  namespace: Django-app-v2
  labels:
    app: Django-app-v2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
