apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flask-test-02-pvc
  namespace: flask-test-02
  labels:
    app: flask-test-02
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
