apiVersion: v1
kind: Service
metadata:
  name: flask-test-02-service
  namespace: flask-test-02
  labels:
    app: flask-test-02
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: flask-test-02
