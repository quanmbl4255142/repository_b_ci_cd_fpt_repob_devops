apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: Django-app-main-ingress
  namespace: Django-app-main
  labels:
    app: Django-app-main
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - Django-app-main.yourdomain.com
    secretName: Django-app-main-tls
  rules:
  - host: Django-app-main.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: Django-app-main-service
            port:
              number: 8000
