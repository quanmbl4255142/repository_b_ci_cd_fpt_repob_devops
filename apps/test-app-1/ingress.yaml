apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-app-1-ingress
  namespace: test-app-1
  labels:
    app: test-app-1
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - test-app-1.yourdomain.com
    secretName: test-app-1-tls
  rules:
  - host: test-app-1.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test-app-1-service
            port:
              number: 8000
