apiVersion: v1
kind: Service
metadata:
  name: nodejs-test-1-service
  namespace: nodejs-test-1
  labels:
    app: nodejs-test-1
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: nodejs-test-1
