apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodejs-test-1-pvc
  namespace: nodejs-test-1
  labels:
    app: nodejs-test-1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
