apiVersion: v1
kind: Service
metadata:
  name: dev-portal-1-service
  namespace: dev-portal-1
  labels:
    app: dev-portal-1
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: dev-portal-1
