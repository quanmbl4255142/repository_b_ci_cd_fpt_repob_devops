apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: django-test-04-ingress
  namespace: django-test-04
  labels:
    app: django-test-04
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod" 
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - django-test-04.yourdomain.com
    secretName: django-test-04-tls
  rules:
  - host: django-test-04.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: django-test-04-service
            port:
              number: 4000
