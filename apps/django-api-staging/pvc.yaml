apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: django-api-staging-pvc
  namespace: django-api-staging
  labels:
    app: django-api-staging
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard

