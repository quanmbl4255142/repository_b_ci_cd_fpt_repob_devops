apiVersion: v1
kind: Service
metadata:
  name: nodejs-tetst-03-service
  namespace: nodejs-tetst-03
  labels:
    app: nodejs-tetst-03
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: nodejs-tetst-03
