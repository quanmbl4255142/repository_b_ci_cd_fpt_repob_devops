apiVersion: v1
kind: Service
metadata:
  name: django-test-09-service
  namespace: django-test-09
  labels:
    app: django-test-09
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: django-test-09
