apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: django-test-v3-pvc
  namespace: django-test-v3
  labels:
    app: django-test-v3
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
