apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: django-test-v3-ingress
  namespace: django-test-v3
  labels:
    app: django-test-v3
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - django-test-v3.yourdomain.com
    secretName: django-test-v3-tls
  rules:
  - host: django-test-v3.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: django-test-v3-service
            port:
              number: 8000
