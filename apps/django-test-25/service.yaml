apiVersion: v1
kind: Service
metadata:
  name: django-test-25-service
  namespace: django-test-25
  labels:
    app: django-test-25
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: django-test-25
