apiVersion: v1
kind: Service
metadata:
  name: Django-app-v3-service
  namespace: Django-app-v3
  labels:
    app: Django-app-v3
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: Django-app-v3
