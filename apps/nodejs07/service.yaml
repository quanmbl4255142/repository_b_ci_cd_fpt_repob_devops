apiVersion: v1
kind: Service
metadata:
  name: nodejs07-service
  namespace: nodejs07
  labels:
    app: nodejs07
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: nodejs07
