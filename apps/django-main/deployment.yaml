apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-main
  namespace: django-main
  labels:
    app: django-main
  annotations:
    argocd-image-updater.argoproj.io/image-list: django-main=ghcr.io/QuanMBL4255142/my-django-app-2
    argocd-image-updater.argoproj.io/write-back-method: git
    argocd-image-updater.argoproj.io/write-back-target: apps/django-main/deployment.yaml
    argocd-image-updater.argoproj.io/django-main.update-strategy: latest
    argocd-image-updater.argoproj.io/django-main.allow-tags: regexp:^.*$
spec:
  replicas: 2
  selector:
    matchLabels:
      app: django-main
  template:
    metadata:
      labels:
        app: django-main
      annotations:
        timestamp: "1759992756"
    spec:
      initContainers:
      - name: init-data-dir
        image: busybox:latest
        command: ['sh', '-c', 'mkdir -p /app/data && chmod 777 /app/data']
        volumeMounts:
        - name: app-data
          mountPath: /app/data
      containers:
      - name: django-main
        image: ghcr.io/QuanMBL4255142/my-django-app-2:latest
        command: ["/bin/sh", "-c"]
        args:
          - |
            python manage.py migrate --noinput
            python manage.py collectstatic --noinput
            gunicorn --bind 0.0.0.0:8000 django_api.wsgi:application
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "sleep 5"]
        ports:
        - containerPort: 8000
        env:
        - name: DJANGO_SETTINGS_MODULE
          value: "django_api.settings"
        - name: PYTHONUNBUFFERED
          value: "1"
        volumeMounts:
        - name: app-data
          mountPath: /app/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/health/
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/health/
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: app-data
        persistentVolumeClaim:
          claimName: django-main-pvc
