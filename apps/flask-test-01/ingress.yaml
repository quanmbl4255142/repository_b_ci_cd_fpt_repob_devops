apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flask-test-01-ingress
  namespace: flask-test-01
  labels:
    app: flask-test-01
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - flask-test-01.yourdomain.com
    secretName: flask-test-01-tls
  rules:
  - host: flask-test-01.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: flask-test-01-service
            port:
              number: 8000
