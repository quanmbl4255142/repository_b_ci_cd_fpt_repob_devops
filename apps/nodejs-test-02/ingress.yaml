apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nodejs-test-02-ingress
  namespace: nodejs-test-02
  labels:
    app: nodejs-test-02
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - nodejs-test-02.yourdomain.com
    secretName: nodejs-test-02-tls
  rules:
  - host: nodejs-test-02.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nodejs-test-02-service
            port:
              number: 8000
