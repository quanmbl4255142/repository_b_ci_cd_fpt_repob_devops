apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: django-app-main-pvc
  namespace: django-app-main
  labels:
    app: django-app-main
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
