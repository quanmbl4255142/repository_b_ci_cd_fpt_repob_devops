apiVersion: v1
kind: Service
metadata:
  name: django-app-main-service
  namespace: django-app-main
  labels:
    app: django-app-main
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: django-app-main
