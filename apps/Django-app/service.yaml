apiVersion: v1
kind: Service
metadata:
  name: Django-app-service
  namespace: Django-app
  labels:
    app: Django-app
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: Django-app
