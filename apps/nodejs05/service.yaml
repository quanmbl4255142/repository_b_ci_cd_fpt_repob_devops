apiVersion: v1
kind: Service
metadata:
  name: nodejs05-service
  namespace: nodejs05
  labels:
    app: nodejs05
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: nodejs05
