apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs05
  namespace: nodejs05
  labels:
    app: nodejs05
    environment: production
  annotations:
    argocd-image-updater.argoproj.io/image-list: nodejs05=ghcr.io/quancv2k4/nodejs_tesst
    argocd-image-updater.argoproj.io/write-back-method: git
    argocd-image-updater.argoproj.io/write-back-target: apps/nodejs05/deployment.yaml
    argocd-image-updater.argoproj.io/nodejs05.update-strategy: latest
    argocd-image-updater.argoproj.io/nodejs05.allow-tags: regexp:^.*$
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nodejs05
  template:
    metadata:
      labels:
        app: nodejs05
        environment: production
      annotations:
        timestamp: "1762147684"
    spec:
      initContainers:
      - name: init-data-dir
        image: busybox:latest
        command: ['sh', '-c', 'mkdir -p /app/data && chmod 777 /app/data']
        volumeMounts:
        - name: nodejs05-data
          mountPath: /app/data
      containers:
      - name: nodejs05
        image: ghcr.io/quancv2k4/nodejs_tesst:latest
        command: ["/bin/sh", "-c"]
        args:
          - |
            node server
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "sleep 5"]
        ports:
        - containerPort: 8000
        env:
        - name: NODE_ENV
          value: production
        - name: ENVIRONMENT
          value: production
        volumeMounts:
        - name: nodejs05-data
          mountPath: /app/data
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/health/
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/health/
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: nodejs05-data
        persistentVolumeClaim:
          claimName: nodejs05-pvc
