apiVersion: v1
kind: Service
metadata:
  name: django-test-1-service
  namespace: django-test-1
  labels:
    app: django-test-1
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: django-test-1
